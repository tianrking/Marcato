Local browser and smoke-test artifacts live here.

Generated screenshots, JSON results, traces, and logs are intentionally ignored
by git. Keep temporary evidence under this directory instead of the project root.

Current generated subdirectories:

- `screenshots/`: Playwright screenshots from smoke, PDF, and performance checks.
- `results/`: JSON metrics emitted by automated browser checks.
